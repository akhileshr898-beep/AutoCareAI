services:
  - type: web
    name: autocare-ai
    runtime: docker
    plan: free
    region: singapore
    healthCheckPath: /health

    envVars:
      - key: SECRET_KEY
        generateValue: true

      - key: DATABASE_URL
        sync: false

      - key: FLASK_ENV
        value: production
